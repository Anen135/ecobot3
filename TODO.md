
## 📜 Техническое задание: 2D-движок на `pygame`

### 📌 Цель

Создать универсальный 2D-движок, реализующий:

* визуализацию объектов;
* управление пространством;
* работу с камерой;
* базовые игровые объекты (движущиеся/статичные).

---

### 📁 Структура проекта (рекомендуемая)

```
project/
├── main.py
├── engine/
│   ├── world.py
│   ├── camera.py
│   ├── entity.py
│   ├── settings.py
│   └── utils.py
├── assets/
└── config/
    └── world_config.json
```

---

### 🧱 Основные компоненты

#### 1. **2D-карта (мир)**

* **Типы пространства:**

  * `ограниченное`: фиксированная ширина/высота, есть границы
  * `циклическое (тор)`: объекты выходят за край — появляются с другой стороны
  * `бесконечное`: условная поддержка, реализуется как бесконечный скролл
* **Размеры** настраиваются (`width`, `height`)

#### 2. **Объекты на карте (Entity)**

* **Базовый класс `Entity`:**

  * координаты `(x, y)`
  * спрайт / цвет
  * размер
  * направление (угол, если нужно)
  * метод `update(dt)` — логика движения
  * метод `draw(surface)` — отрисовка

* **Типы объектов:**

  * **Статичные** — еда, препятствия
  * **Динамичные** — агенты, враги

#### 3. **Камера**

* Поддерживает 3 режима:

  * `следить за агентом`
  * `следить за едой`
  * `фиксированное положение`

* Функции камеры:

  * Центрирование на объекте
  * Масштаб (в перспективе)
  * Ограничения по краям мира (если они есть)

#### 4. **Окно и визуализация**

* Возможность переключения:

  * `оконный режим` (с заданным разрешением)
  * `полноэкранный режим` (`pygame.FULLSCREEN`)
* Поддержка FPS-лимита
* Отображение служебной информации (debug-панель)

#### 5. **Система времени**

* Управление временем (`dt`)
* Поддержка ускоренного режима (в симуляциях)
* Таймеры, шаги обновления

---

### 🧰 Настройки (configurable):

* Размер окна / fullscreen
* Цвет фона
* Размер мира
* Начальные объекты
* Видимые слои
* Параметры камеры

---

### 🎯 Дополнительно (опционально):

* Слои отрисовки (фон, объекты, интерфейс)
* Грид-сетка или фон-текстуры
* Коллизии (если пригодится)
* Простая UI-панель (счётчики, FPS)

---

### 🧪 Тестовый запуск:

* Мир 1000x1000
* Один агент с направлением
* 10 объектов еды, раскиданных по карте
* Камера следует за агентом
* Возможность включить/выключить тор

---



### 🎨 Задача: Внедрение системы слоёв отрисовки и выделение UI

---

#### 📌 Цель:

Обеспечить структурированную и управляемую отрисовку объектов и интерфейса путём:

1. Введения системы **слоёв отрисовки** (`layer`) для игровых объектов.
2. Выделения **пользовательского интерфейса (UI)** в отдельный модуль `ui.py`, отделённый от мира и сущностей.

---

### 🧱 Часть 1: Система слоёв отрисовки

#### 📁 Требования:

1. **Поле `layer` в классе `Entity`:**

   * Тип: `int`
   * Значение по умолчанию: `0`
   * Интерпретация: чем больше значение, тем выше "поверх" объект отрисовывается

2. **Сортировка в `World.draw(surface, camera_offset)`**:

   * Перед отрисовкой все сущности сортируются по `layer`
   * При режиме `torus` сортировка происходит **до** генерации обёрнутых позиций

3. **Рекомендованные уровни слоёв:**

   * `-1` — фоновая графика, тени, декоративные элементы
   * `0` — основные игровые объекты (агенты, еда, препятствия)
   * `1+` — эффекты, вспышки, всплывающие надписи и т.п.

4. **UI не является частью системы сущностей**, и **не включается в `world.draw`** — он отрисовывается **после** всего мира поверх остальных слоёв.

---

### 🖥️ Часть 2: Выделение UI в отдельный модуль `ui.py`

#### 📁 Файл: `engine/ui.py`

Создаётся отдельный модуль, который:

* Содержит функции отрисовки UI-панелей, текстов, FPS и другой информации
* Может использовать `pygame.font`, `pygame.Surface`, но **не зависит от сущностей и мира**
* Работает напрямую с `screen` (основной surface), **после вызова `world.draw()`**

#### 📌 Почему UI отдельно:

* UI — это **интерфейс игрока**, а не часть симуляции или игрового мира.
* Он не зависит от камеры, координат объектов и `camera_offset`.
* Отделение UI позволяет:

  * переиспользовать и масштабировать его независимо от логики мира
  * избегать загрязнения `world.draw()` логикой, не связанной с геймплеем

### ✅ Критерии приёмки:

* Объекты отрисовываются строго в порядке `layer`
* UI рендерится поверх всех игровых объектов
* Отделение UI в `ui.py` не нарушает логику `World` и `Entity`
* UI не зависит от камеры и корректно отображается при любом сдвиге мира

---

| №  | Компонент                                       | Подзадача / Описание                                                                 | Статус       |
|----|--------------------------------------------------|----------------------------------------------------------------------------------------|--------------|
| 1  | Структура проекта                                | Основные файлы и каталоги по шаблону                                                  | выполнено |
| 2  | 2D-карта (мир)                                   | Реализация ограниченного пространства                                                 | выполнено |
| 3  |                                                  | Реализация циклического мира (тор)                                                    | выполнено |
| 4  |                                                  | Реализация бесконечного пространства (условно)                                        | выполнено |
| 5  |                                                  | Поддержка настройки размеров мира                                                     | Настройка в JSON файле конфигурации |
| 6  | Объекты (Entity)                                 | Базовый класс с координатами, размером, спрайтом, углом                               | Нет спрайта |
| 7  |                                                  | Метод `update(dt)`                                                                    | выполнено |
| 8  |                                                  | Метод `draw(surface)`                                                                 | выполнено |
| 9  |                                                  | Статичные объекты (еда, препятствия)                                                  | только еда |
| 10 |                                                  | Динамичные объекты (агенты, враги)                                                    | только агент |
| 11 | Камера                                           | Режим: следить за агентом                                                             | выполнено |
| 12 |                                                  | Режим: следить за едой                                                                | выполнено |
| 13 |                                                  | Режим: фиксированная позиция                                                          | выполнено |
| 14 |                                                  | Ограничение по краям мира                                                             | выполнено |
| 15 |                                                  | Центрирование и сдвиг камеры                                                          | выполнено |
| 16 | Окно и визуализация                              | Оконный режим                                                                         | выполнено |
| 17 |                                                  | Полноэкранный режим                                                                   | выполнено |
| 18 |                                                  | FPS-лимит                                                                             | выполнено |
| 19 |                                                  | Debug-панель (отображение FPS, инфо и т.д.)                                           | выполнено |
| 20 | Система времени                                  | Расчёт `dt` для `update()`                                                            | выполнено |
| 21 |                                                  | Поддержка ускоренного режима                                                          | выполнено |
| 22 |                                                  | Встроенные таймеры, шаги                                                              | выполнено как utils.py |
| 23 | Настройки (configurable)                         | Настройка размеров окна и fullscreen                                                  | Настройка в JSON файле конфигурации |
| 24 |                                                  | Настройка цвета фона                                                                  | Настройка в JSON файле конфигурации |
| 25 |                                                  | Размер мира, параметры камеры, слои                                                   | Настройка в JSON файле конфигурации |
| 26 |                                                  | Инициализация начальных объектов из конфигов                                          | ☐ Не выполнено | X
| 27 | Система слоёв отрисовки                          | Свойство `layer` в `Entity`                                                           | выполнено |
| 28 |                                                  | Сортировка по `layer` в `world.draw()`                                                | выполнено |
| 29 |                                                  | Поддержка слоёв: -1 (фон), 0 (объекты), 1+ (эффекты)                                  | выполнено |
| 30 | UI                                               | Модуль `ui.py`, отделён от `World`                                                    | выполнено |
| 31 |                                                  | UI отрисовывается поверх `World`, независимо от камеры                                | выполнено |
| 32 |                                                  | Использование `pygame.font`, `pygame.Surface`                                         | выполнено |
| 33 | Тестовый запуск                                  | Карта 1000x1000, агент, 10 объектов еды                                               | выполнено |
| 34 |                                                  | Камера следует за агентом                                                             | выполнено |
| 35 |                                                  | Возможность включения/отключения тор                                                  | Настройка в JSON файле конфигурации |

---

## 📌 ТЗ: Загрузка объектов из конфигурационного файла

### 🎯 Цель:

Обеспечить возможность **инициализировать объекты мира (агенты, еда, препятствия и т.п.) из внешнего JSON-файла**, чтобы легко:

* менять стартовую расстановку;
* создавать пресеты сценариев;
* использовать при обучении/тестировании ИИ.

---

### 📁 Структура конфигурационного файла

#### 🔸 Пример: `config/world_objects.json`

```json
{
  "objects": [
    {
      "type": "agent",
      "x": 100,
      "y": 200,
      "angle": 90,
      "id": "agent_1"
    },
    {
      "type": "food",
      "x": 300,
      "y": 400
    },
    {
      "type": "food",
      "x": 500,
      "y": 100
    },
    {
      "type": "obstacle",
      "x": 600,
      "y": 600,
      "width": 50,
      "height": 50
    }
  ]
}
```

---

### 🔧 Требования к реализации

#### 1. **Общий метод загрузки**

Создать метод:

```python
world.load_from_config(filepath: str)
```

* Парсит JSON
* Создаёт объекты нужных типов (по `"type"`)
* Добавляет их в мир (`world.add_entity()`)

---

#### 2. **Поддерживаемые типы объектов**

| Тип        | Класс      | Обязательные поля           | Опциональные поля       |
| ---------- | ---------- | --------------------------- | ----------------------- |
| `agent`    | `Agent`    | `x`, `y`                    | `angle`, `id`, `sprite` |
| `food`     | `Food`     | `x`, `y`                    | —                       |
| `obstacle` | `Obstacle` | `x`, `y`, `width`, `height` | `color`, `id`           |

---

#### 3. **Регистрация типов объектов**

Чтобы можно было добавлять новые типы, реализовать простую фабрику:

```python
ENTITY_REGISTRY = {
    "agent": Agent,
    "food": Food,
    "obstacle": Obstacle
}
```

При загрузке:

```python
entity_cls = ENTITY_REGISTRY[obj_data["type"]]
entity = entity_cls(**params)
```

---

#### 4. **Обработка ошибок**

* Если в JSON указано неизвестное поле или тип — логировать предупреждение, но не падать.
* Если координаты выходят за границы мира — логировать и игнорировать/ограничивать.

---

#### 5. **Поддержка конфигурации нескольких миров**

В будущем можно будет сделать поддержку нескольких пресетов, например:

```bash
python main.py --config config/scenario_3.json
```

---

### ✅ Пример использования

```python
world = World(...)
world.load_from_config("config/world_objects.json")
```

---

### 📎 Вспомогательная структура кода

```python
# world.py
class World:
    def load_from_config(self, path):
        with open(path) as f:
            data = json.load(f)
        for obj in data["objects"]:
            obj_type = obj.get("type")
            if obj_type not in ENTITY_REGISTRY:
                print(f"[!] Unknown type: {obj_type}")
                continue
            cls = ENTITY_REGISTRY[obj_type]
            entity = cls(**obj)
            self.add_entity(entity)
```

---

## 📌 Что считается успешным выполнением:

* При запуске мира из JSON — на экране отображаются объекты, как в конфиге.
* Можно легко заменить файл — и получить новый сценарий.
* Не падает при отсутствии необязательных полей.

---